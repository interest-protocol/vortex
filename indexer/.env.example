# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017
MONGODB_DATABASE=vortex

# Sui Network Configuration
# Options: mainnet, testnet, devnet
SUI_NETWORK=testnet

# Vortex Package Address
# Default testnet package: 0xcf81b96e392f82b776ee980108357426b726c4043c838822545a307e12c5ded6
VORTEX_PACKAGE=0xcf81b96e392f82b776ee980108357426b726c4043c838822545a307e12c5ded6

# Checkpoint Data Source (REQUIRED - choose ONE method)
# Method 1: Remote Store (recommended for testnet/mainnet)
REMOTE_STORE_URL=https://checkpoints.testnet.sui.io

# Method 2: Local Ingestion (for local testing with checkpoint files)
# LOCAL_INGESTION_PATH=/path/to/checkpoints

# Method 3: RPC API (alternative data source)
# RPC_API_URL=https://fullnode.testnet.sui.io:443
# RPC_USERNAME=
# RPC_PASSWORD=

# Optional: gRPC streaming endpoint for real-time data
# STREAMING_URL=https://fullnode.testnet.sui.io:443

# Indexer Configuration
# Optional: Starting checkpoint number (overrides saved checkpoint)
# FIRST_CHECKPOINT=

# Optional: Ending checkpoint number (for backfills)
# LAST_CHECKPOINT=

# Optional: Only run specific pipelines (comma-separated)
# PIPELINE=new_commitments,new_pools

# Optional: Task name for running multiple indexers (requires READER_INTERVAL_MS)
# TASK=backfill
# READER_INTERVAL_MS=1000
